#include <stdio.h>
/**
 * main - imprime la phrase avec une grammaire correcte,
 * mais le résultat est une œuvre d'art,
 * Return: 0 si la sortie s'est faite correctement.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
